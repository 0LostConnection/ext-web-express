{
  "name": "ext-web-express",
  "private": true,
  "description": "Monorepo raiz — dependências e scripts do backend ficam em backend/",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "install:backend": "npm install --prefix backend"
  }
}
